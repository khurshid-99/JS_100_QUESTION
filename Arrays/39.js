// 39. Sort an array of numbers in ascending order using a custom comparator function and print the sorted array.

const numbers = [8, 3, 10, 1, 6, 2];

numbers.sort((a, b) => a - b);
console.log(numbers);
